I now have well-reconciled material from six quality search passes (StockCharts, Fidelity, Schwab, CMT Association, TradingSim, practitioner/institutional writing). WebFetch's socket failure cost me the full-text primary pages, but the search summaries gave consistent, cross-corroborated findings including the honest skeptical record. I'll note the fetch limitation in the summary. Writing the lesson now.

This fills the **zero-coverage `macro_regimes` discipline** — the single most foundational step (regime read, step #1) with no lesson behind it — and maps directly onto the bundle's `market_breadth` block.

```json
{"lesson": {"discipline": "macro_regimes", "topic": "Market breadth as the regime aggression dial: participation, the divergence/thrust asymmetry, and why breadth is a CONDITION not a timing signal", "summary": "Breadth measures how many stocks are PARTICIPATING, which no index level can reveal - an index at new highs on narrowing participation and one on broadening participation look identical in SPY but are different regimes. The classic bearish read is a breadth DIVERGENCE: the index prints a new high while the advance-decline line makes a lower high and the share of stocks above their 200-DMA falls, meaning the advance is being carried by fewer, larger names. Documented cases precede 1987, 2000, 2007 and 2021 tops (the NYSE A-D line peaked nearly two years before the 2000 Nasdaq crash). BUT the honest empirical record on top-CALLING is weak: a divergence is a condition, not a timing signal - it can persist for weeks, months, or over a year, produces frequent false positives during ordinary corrections, and tops are inherently hard to time. Practitioner consensus is to treat divergence as a yellow flag and wait for a PRICE trigger (a 50-DMA or trendline break) before acting. The important asymmetry: bullish breadth THRUSTS off washout lows (Zweig-type surge from <40% to >61.5% of stocks advancing within ~10 days) are far more reliable than bearish divergences - they are rare (~18 since 1945) but historically preceded ~23% average 12-month S&P gains with a near-perfect record, because bottoms follow repeatable patterns and the market drifts up over time (fewer false positives than tops). On the level side, the share of stocks above the 200-DMA is the regime gauge: >70% = broad healthy uptrend, 30-70% = neutral/mixed, <30% = broken, and stretched extremes >85-90% often precede a pullback (stretched, not a sell signal). The 2023-24 mega-cap episode is the cleanest recent illustration - indices at highs while stocks above their 50-DMA fell from ~80% to ~40% and a handful of names carried the tape. I could not fetch the full primary pages (proxy socket failures) so this rests on cross-corroborated search summaries rather than the original long-form articles.", "key_points": ["Breadth measures participation the index level hides; narrowing participation (A-D lower high vs index new high, falling % above 200-DMA) is the top-warning pattern but historically an unreliable TIMING tool", "A breadth divergence is a CONDITION, not a timing signal - it can persist for months and gives many false positives; use it to lower aggression, never as a standalone short or stand-down. Wait for a price trigger (50-DMA break) to act", "Asymmetry: bullish breadth THRUSTS off lows are far more reliable than bearish divergences at tops - bottoms follow repeatable patterns, tops do not; the thrust is the higher-confidence, press-harder signal", "% of stocks above 200-DMA is the regime gauge: >70% broad uptrend, 30-70% neutral, <30% broken; >85-90% is stretched (pullback risk, not a sell)", "pct_beating_spy_1m < ~40% means the index is carried by a minority however healthy the level looks - the sharpest single fragility read (2023-24: % above 50-DMA fell ~80%->40% under narrow mega-cap leadership)", "Empirical caveat is genuine: divergences 'called' 1987/2000/2007/2021 but also fired falsely often; only combine with price confirmation"], "how_to_apply": "In the regime step (step #1, the aggression dial), read market_breadth.universe FIRST for participation, starting with pct_beating_spy_1m: under ~40% treat the tape as neutral-to-fragile even when benchmark_trend reads supportive, and shift toward smaller size and a different KIND of setup (catalyst, value, diversifier) rather than more of the leadership - exactly the 'act cautious' response CLAUDE.md and momentum_health 'softening' already prescribe. Treat a breadth divergence (index near highs while pct_above_200dma is falling / advance_decline_ratio deteriorating) as a yellow flag that lowers aggression, NEVER as a reason to reject an otherwise-clean fat pitch and never as a stand-down - it is a condition not a timing signal and the only hard regime block is the SPY-below-200-DMA regime gate (this mirrors KB-4213529672: a standing condition must not become an unwritten veto). Conversely, weight bullish breadth EXPANSION as the higher-confidence asymmetric signal: when pct_above_200dma crosses back above 50% and pct_beating_spy_1m rises through 40-50% (a broadening/thrust regime), that is when to press hardest and take full-size fat pitches. Cross-check sector_rotation_30d (accelerating vs decelerating) for WHERE participation is rotating, and always keep the two-sample discipline: never cite market_breadth.universe (AI-biased ~184 names) as market breadth, and read a null in market_breadth.broad as 'not measured', not zero.", "sources": ["https://chartschool.stockcharts.com/table-of-contents/market-indicators/advance-decline-line", "https://chartschool.stockcharts.com/table-of-contents/market-indicators/percent-above-moving-average", "https://www.fidelity.com/learning-center/trading-investing/advance-decline", "https://www.schwab.com/learn/story/breadth-check-strength-and-weakness-trend-tracker", "https://www.athrasher.com/what-you-may-be-missing-when-evaluating-market-breadth/", "https://content.cmtassociation.org/a/spotting-a-market-bottom", "https://www.indexbox.io/blog/zweig-breadth-thrust-indicator-a-rare-bullish-signal/", "https://thesisrationale.substack.com/p/market-breadth-the-indicator-most"]}}
```

**Why this topic, and the contradiction check:**

- **Topic choice:** I overrode the suggested `technical_analysis` (already two well-cited lessons: √t stops KB-2388376284, base/pivot KB-9692173165) in favor of the **only zero-coverage discipline, `macro_regimes`** — which is the *first* step of every trading run and the aggression dial that gates all sizing. The bundle hands me a rich `market_breadth` block that no existing lesson taught me to synthesize.
- **No conflict declared.** Nothing in the knowledge base touches market-level regime/breadth, so this lesson stands alone. The "condition, not timing signal — wait for price confirmation" theme *rhymes* with KB-2665689010 and KB-2596574918 (single-name confirmation gating) but operates at a different level (market vs. name), so it complements rather than contradicts them. I omitted the `conflicts` field.
- **Honesty note:** I flagged inside the summary that WebFetch socket failures prevented reading the full primary articles, so the lesson rests on cross-corroborated search summaries — and I kept the genuinely mixed empirical record (divergences call tops *and* fire falsely) front and center rather than overselling breadth as predictive.

**Improvement note:** WebFetch failed on every call this run with "Socket is closed," and my proxy-status diagnostic was blocked by the Bash sandbox's env-var-expansion guard (`$HTTPS_PROXY` triggered a `Contains expansion` refusal). A study session leans heavily on reading full source pages; a documented fallback — e.g. allowing the proxy-status curl to run with `dangerouslyDisableSandbox` for read-only diagnostics, or a WebFetch retry-with-backoff — would let future study runs recover primary-source depth instead of degrading to search-summary-only evidence.