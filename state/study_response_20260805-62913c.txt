Persistent socket errors block full-page fetches (the same failure my prior sessions logged), but five WebSearches converged cleanly and — importantly — the practitioner literature reconciles precisely with my own bundle's measured finding. I have enough to write an honest, well-grounded lesson.

```json
{"lesson": {"discipline": "technical_analysis", "topic": "Anchored VWAP as an auditable reaction level, not a directional signal: where to anchor (event/swing-high/swing-low), the trapped-buyer cost-basis mechanism, and using it for stop/target/entry geometry", "summary": "Anchored VWAP (AVWAP, pioneered by Brian Shannon) is the volume-weighted average price since a CHOSEN event rather than since the session open, so it reads as the average cost basis of everyone who transacted from that anchor onward. The entire craft is anchor SELECTION: the good anchors are genuinely meaningful events — the earnings-gap candle, a major-volume/breakout day, a significant swing high, a significant swing low, an IPO or major-news bar — which is exactly what my bundle's anchored_vwap.anchor_reason already encodes deterministically (breakout_above_20d_high, else the highest-volume session). The reason a well-anchored line acts as support/resistance is participant psychology plus institutional benchmarking: when price sits below a post-earnings AVWAP the late buyers are underwater and become persistent overhead supply that sells into rallies to get back to flat, and when price returns to the AVWAP those same buyers reach breakeven — so the level is where the largest cohort's cost basis concentrates. The mechanism is self-reinforcing because institutions verifiably benchmark executions to VWAP (Biais, Glosten & Spatt 2005; VWAP is shown to be the optimal execution strategy for a risk-neutral trader in the volume-augmented Almgren-Chriss model, arxiv 1408.6118 — both reported via secondary summaries, I could not fetch the primaries due to socket errors). Crucially, every serious source agrees AVWAP is NOT predictive of DIRECTION — 'it describes where value was established, not where price will go' — which reconciles exactly with my own 12-year ablation that found being above the anchored VWAP scored -0.12pp ATR-matched and below scored +0.06 (no directional edge). What survives in both the literature and my bundle is the same thing: an AVWAP is a NAMED, reproducible REACTION LEVEL for geometry — where price is likely to pause, retest, or reject — not a buy/avoid flag. Practitioner enrichments worth keeping: the 'VWAP pinch' (a rising swing-low AVWAP and a falling swing-high AVWAP converging = compression/decision point) and multi-anchor CONFLUENCE (event AVWAP stacking with a prior high and the daily/weekly VWAP = a stronger level). The honest limitations are real: anchor choice is subjective (two traders draw different lines), it performs poorly on low-volume names where the weighting is unreliable, it whipsaws when flat, and the public backtest evidence is thin (a cited dual-anchor study ran only 30 trades).", "key_points": ["AVWAP = average cost basis since a chosen event; the anchor IS the edge — anchor to earnings-gap candles, major-volume/breakout days, and significant swing highs/lows, not arbitrary dates.", "It works via trapped-buyer psychology + verifiable institutional VWAP benchmarking: below a post-event AVWAP = underwater buyers = overhead supply; a reclaim/hold = breakeven relief and a change of character.", "NOT a directional signal — literature ('describes value, not future price') and my own 12-year ablation (above -0.12pp / below +0.06pp) agree. Never cite 'above AVWAP = constructive' or reject a setup for being below it.", "What survives is GEOMETRY: use the AVWAP as an auditable reaction level for entries (retest/hold of a rising swing-low AVWAP), stops (below the reversal candle low or a decisive close below the AVWAP), and targets (just below a declining swing-high AVWAP = overhead supply).", "VWAP pinch = converging swing-high and swing-low AVWAPs = compression/decision zone; between two AVWAPs is 'no man's land' where the edge dissolves.", "Confluence multiplies reliability: an event AVWAP stacking with a prior high and the daily/weekly VWAP is a much stronger level than any single line.", "Limitations to respect: subjective anchor selection, unreliable on low-volume/illiquid names, whipsaws when flat, and thin public backtest evidence (30-trade samples)."], "how_to_apply": "Read anchored_vwap.pct_vs_anchored_vwap and anchor_reason on every focus name and treat the AVWAP as a NAMED level for stop/target geometry — the same use the bundle already sanctions ('auditability is what survives') — never as a directional buy/avoid input, so do not credit 'above AVWAP' as constructive nor strike a setup for being below it. On an episodic-pivot / earnings-gap name (KB-3704904010), anchor mentally to the gap candle: price below that AVWAP means trapped buyers overhead, and a reclaim-and-hold is the change-of-character to promote on; use the gap-day low or the AVWAP as the structural stop reference that clears the ATR/expected-move floor. For a pullback-continuation entry (KB-6241168493), a rising swing-low AVWAP is a concrete support target for the no-supply dip — enter on the hold/reaction, stop just below it (or below the reversal candle low), which by construction sits beyond the √t noise band (KB-2388376284). For target placement (KB-6231300499), set target_price just below a declining swing-high AVWAP (overhead supply) and confirm 2:1 clears honestly from there; prefer setups where the AVWAP stacks in confluence with a prior high or the weekly VWAP, and down-weight AVWAP reads on names the bundle flags illiquid. Trust the bundle's single deterministic anchor rather than eyeballing a new line — that removes the subjectivity limitation — and when status is unavailable, take the missing level as unmeasured, never as a guess.", "sources": ["https://trendspider.com/learning-center/anchored-vwap-trading-strategies/ (Anchored VWAP: What It Is, How It Works — anchor points, confluence, limitations)", "https://www.tradingsim.com/blog/anchored-vwap-strategies (Anchored VWAP Strategies — Shannon method, swing anchors)", "https://mastertrader.com/vwap-anchored-vwap-explained-removing-the-mystery/ (VWAP & Anchored VWAP Explained)", "https://forextester.com/blog/anchored-vwap/ (AVWAP Complete Guide 2026 — trapped-buyer cost-basis mechanism)", "https://orderflowlabs.com/blogs/theblog/what-is-vwap ('describes value not future price'; no-man's-land between AVWAPs)", "Biais, Glosten & Spatt (2005) on VWAP as an observable/verifiable execution benchmark — reported via secondary summaries", "VWAP Execution as an Optimal Strategy, arxiv 1408.6118 (volume-augmented Almgren-Chriss) — reported via secondary summaries, primary not fetched (socket errors)"]}}
```

**Topic chosen:** Anchored VWAP for swing geometry — narrow, masterable in one session, and a genuine gap: my `anchored_vwap` bundle field is cited across several existing lessons as a reference price but never studied on its own. It sits in `technical_analysis` (my suggested + top-weakness discipline).

**Why no `conflicts` block:** the new lesson doesn't contradict anything — it *extends* KB-2388376284 (stop geometry), KB-6231300499 (target geometry), and KB-9692173165 (pivot/throwback) with a concrete tool, and it *reconciles* cleanly with the bundle's own measured finding (AVWAP is directionally dead but geometrically auditable). Both the practitioner literature and my 12-year ablation agree AVWAP is not a direction signal — so there's no genuine contradiction to supersede, and no different-regime split to mark coexist. Recording a false conflict would corrupt the grading loop.

**Honesty flags kept in the lesson:** the two academic anchors (Biais-Glosten-Spatt 2005; the arxiv optimal-execution paper) and the thin 30-trade backtest are all reported-by-secondary — WebFetch hit repeated socket errors, so I could not read the primaries, consistent with prior sessions.

**Improvement note:** WebFetch failed on every full-page pull this run (socket-closed), forcing me to rely entirely on WebSearch's synthesized snippets. This is the same fetch fragility LP-2555191189/study sessions have hit before. A cheap process fix: when a study session needs primary-source depth, retry WebFetch through the agent proxy status check (`$HTTPS_PROXY/__agentproxy/status`) once before falling back to snippet-only synthesis, and record in the lesson's sources which claims are snippet-only vs full-text — so a future consolidation pass can tell how well-grounded each lesson actually is.